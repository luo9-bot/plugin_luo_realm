# Luo Realm Rust 项目开发规则

本文件适用于当前目录及其全部子目录。所有后续修改都必须遵守以下规则。

## 硬性要求

1. 必须持续优化代码结构，不得以“当前可运行”为由堆叠临时代码。
2. 模块必须职责清晰。业务规则、数据库访问、HTTP 路由、认证和界面代码不得相互混杂。
3. 函数应只完成一项明确工作；复杂流程应拆成命名准确、输入输出清晰的函数或类型。
4. 优先使用 Rust 类型系统表达约束，包括枚举、新类型、`Option`、`Result`、模式匹配、trait 和生命周期；不得用无意义字符串或布尔参数代替明确类型。
5. 错误必须显式传播或处理。生产代码不得使用可能因外部输入、文件、网络或数据库状态触发的 `unwrap`、`expect` 或 `panic`。
6. 保持所有权和借用关系简单，避免无必要的 `clone`、全局可变状态、共享锁和动态分发。
7. 修改完成后必须运行 `cargo fmt --check`、`cargo clippy --all-targets -- -D warnings` 和与修改范围相符的测试或构建检查。

## 破坏性重构原则

1. 本项目是个人开发项目，不要求兼容旧实现、旧内部 API、旧战报结构或尚未正式发布的数据格式。
2. 当旧结构妨碍正确的领域建模或后续扩展时，应直接删除或重写，不得为其保留兼容门面、适配器、双写、降级分支或过渡性抽象。
3. 数据库结构可以通过明确的新迁移直接演进；除非用户单独要求保留历史数据，否则不得为了推测中的兼容需求增加迁移负担。
4. 重构完成后只能保留一条权威实现路径，不得让新旧战斗、技能、装备或渲染逻辑长期并存。
5. 破坏性重构不等于降低质量；输入验证、错误处理、事务原子性、确定性计算和突然中断恢复能力仍必须完整保留。

## 迭代器与函数式写法

1. 对集合进行纯转换、筛选、查找、聚合或收集时，必须优先使用 `iter`、`into_iter`、`map`、`filter`、`filter_map`、`find`、`find_map`、`fold`、`collect`、`for_each` 或 `try_for_each`。
2. 能直接表达为迭代器链的循环不得手写。例如“遍历后 `push` 到新 `Vec`”应写成 `map/filter_map + collect`，顺序执行并传播错误应使用 `try_for_each`。
3. 迭代器链应保持短小并使用有意义的方法名。若闭包嵌套使控制流更难理解，应先提取命名函数。
4. 状态机、需要 `break/continue` 的算法、端口重试、协议解析、像素级可变写入等场景，在显式循环更清晰时允许保留循环；保留原因必须能从代码结构直接看出，复杂情况需添加简短注释。
5. 不得为了“函数式”而引入额外分配、隐藏事务顺序、吞掉错误或降低可读性。

## Git 禁止项

1. 永远不得执行 `git push`。
2. 永远不得将 `.codex/` 及其任何内容加入 Git。
3. 永远不得将 `.docs/` 及其任何内容加入 Git。
4. 永远不得将任何测试相关文件加入 Git，包括但不限于：
   - `test/`、`tests/` 目录及其全部内容；
   - 任意层级的 `test_*.rs`、`*_test.rs`、`*_tests.rs` 和 `tests.rs`；
   - 测试夹具、测试快照、测试输出和仅供测试使用的辅助脚本。
5. 执行 `git add` 时必须使用明确文件路径，不得使用 `git add .`、`git add -A` 或会覆盖上述禁止项的强制添加命令。
6. 提交前必须检查暂存区；发现禁止项时应从暂存区移除，但不得删除用户本地文件。

## 可读性与维护性

1. 名称必须表达领域含义，避免单字母变量、含糊缩写和魔法值。
2. 公共 API 使用文档注释说明约束、错误和持久化语义；实现注释只解释不明显的原因，不复述代码。
3. 优先复用标准库、现有领域函数和数据库约束，不创建只有一个实现的抽象层。
4. 涉及钱包、玩家状态、修行、物品、权限和审计的修改必须保持事务原子性，并在信任边界验证输入。
5. 文件替换、Token 轮换、SQLite WAL 和在线备份必须保留突然中断后的恢复能力。
